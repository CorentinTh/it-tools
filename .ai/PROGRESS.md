# Implementation Progress

Last updated: 2026-08-16

This is the live implementation journal for the roadmap. A task is `DONE` only
after its relevant regression tests and acceptance gates pass on the current
working tree. `IN PROGRESS` means implementation exists but the complete
milestone Definition of Done has not yet passed.

## Resume checkpoint

Cross-machine and cross-session recovery starts in `.ai/HANDOFF.md`. The last
completed implementation checkpoint is `5f7e97a`; it was verified on the live
`origin/feat-ai-research` ref on 2026-08-07 before the documentation-only
handoff update. The full gates below were last executed on 2026-07-18; the
transfer review verified repository/document consistency but did not relabel
those measurements as newly rerun.

## Current slice

| Area | Status | Current result / remaining gate |
|---|---|---|
| M0 quality baseline | DONE | Current zero-warning lint, dual-project typecheck, 1438/1438 unit tests across 253 files, production build, and 412/412 artifact budgets pass. New-feature production/source-dev flows pass 4/4, and the fresh sequential generated-registry smoke passes all 131 routes without runtime/chunk errors or optimizer restart. |
| UI consistency foundation | DONE | Shared tokens, route-level control adapters, vertical non-diff transformers, generator/task/form/reference archetypes, and source ratchets are active across the catalog. The executable 131-route desktop/light plus mobile/dark matrix covers overflow, visible form-control names, theme, content, and runtime/chunk failures. Seven deterministic Darwin Chromium baselines span all five core archetypes plus true diff while their semantic assertions remain cross-platform. |
| M0 build measurements | DONE | Schema-v4 baseline includes literal route-owned workers in every closure; schema-v1 budgets enforce shell, required Workbox entries, default routes, rationale-backed heavy-route ceilings, and independent reviewed worker ceilings in CI/release. Sixteen infrastructure tests and 412 current-artifact checks pass; host build-time/modules/RSS telemetry remains informational. |
| PWA mandatory install | DONE | Shell-only Workbox inventory is 9 files / 899,808 B raw / 297,798 B gzip. Required membership and <=1 MB raw / <=350 kB gzip / <=10 files fail CI/release; 100,192 B raw headroom remains after the 131-route wave. Clean-HTTP-cache offline lazy-route reload and File Hash worker reuse remain covered. |
| Generated tool registry | DONE | Typed per-tool category/order metadata feeds one deterministic generated registry for routes, categories, Home/search, favorites, scaffolding, and smoke traversal. Build/tests reject stale output and duplicate path/category positions; lazy component imports and current URLs remain intact. The all-route smoke no longer requires an exact manually synchronized count. |
| Emoji true windowing and search | DONE | Responsive row windowing mounts only the visible slice plus one overscan row while preserving headers, total geometry, ARIA position/set metadata, and keyboard movement across unmounted items. Fuse remains inside a cancellable disposable worker; repeated production 4x-CPU runs pass the strict `<50 ms` Long Task gate. |
| JWK/JWKS asymmetric JWT verify | DONE | The existing JWT route verifies bounded local public JWK/JWKS/SPKI for RSA-PKCS1, RSA-PSS, ECDSA, and EdDSA with exact `kid` selection, strict key/header/signature checks, Web Crypto execution, no network fetch, and no persistence. |
| OpenAPI Inspector | DONE | Added one lazy bounded disposable-worker route for structural OpenAPI 3.0/3.1 JSON/YAML checks, local-reference resolution, endpoint inventory, curl examples, and mock request payloads. External references are report-only and never fetched; full conformance validation is explicitly not claimed. |
| PKCS#12 / PEM Workspace | DONE | Added one lazy certificate-only workspace using exact `pkijs@3.4.0` after license/audit/bundle review. Modern PFX integrity and protected safe contents are processed in a disposable worker; certificates may be exported, private-key bags are only counted, and public PEM accepts certificates/CSRs/public keys. The 399,673/87,264 B worker passes a 420/95 kB gate and PKIjs is explicitly prebundled for reload-free source dev. |
| XLSX Reader | DONE | Added one lazy local explicit-action Data route using exact `saxen@11.1.1`, native raw-DEFLATE, and a repository-owned ZIP/OPC boundary in a disposable worker. Macro-free workbook metadata, selected-sheet paging, exact scalar lexemes, cached-formula disclosure, conservative JSON/CSV export, strict ZIP/XML/page/output/time bounds, session-only privacy, and independent route/worker budgets pass. |
| Parquet Reader | DONE | Added one lazy local explicit-action Data route with exact dependency-free `hyparquet@1.28.2`, Blob-backed selected reads in a disposable worker, strict metadata/chunk/page/output/time/memory bounds, schema and row-group inspection, 200-row/16-column paging, conservative JSON/CSV export, UNCOMPRESSED/SNAPPY support, encrypted/external-layout rejection, session-only privacy, and independent route/worker budgets. |
| Mermaid Diagram Renderer/Exporter | DONE | Added one lazy local explicit-action Development route with five allowed syntax families, fixed strict/deterministic configuration, pre-render active-content rejection, post-render DOMPurify plus SVG/resource bounds, a scriptless network-denying sandbox, and sanitized SVG/PNG actions. Source/output are ephemeral; late results are invalidated while the synchronous DOM residual is disclosed. Exact patched Mermaid 10.9.8 and Lodash 4.18.1 resolve with zero target advisories; no upstream implementation code was copied. |
| DNS-over-HTTPS Query | DONE | Added one lazy explicit-network Network route using RFC 8484 binary POST and a fixed two-mode Cloudflare allow-list. DNS wire, response, output, timeout, replacement, cancellation, and disposal are bounded; names/results stay out of URL/storage/analytics/application logs; the UI discloses resolver observability and makes no anonymity/ODoH claim. No runtime dependency, worker, or upstream code was added. |
| Markdown Diff | DONE | Added one lazy local true-diff route with bounded line/word source alignment in a disposable worker, equal peer inputs, and optional last-successful previews with raw HTML/links/images disabled plus a static DOMPurify allow-list. Content is ephemeral, DOMPurify is exactly pinned at patched `3.4.13`, and no editor/diff dependency or upstream implementation code was added. |
| Argon2id Hash & Verify | DONE | Added one lazy local Crypto route with pinned `hash-wasm@4.12.0` only in a disposable worker, Web Crypto salts, RFC 9106 memory-constrained defaults, strict canonical Argon2id `v=19` PHC parsing, bounded resources/protocol/lifecycle, ephemeral sensitive values, independent compatibility fixtures, and source-dev hash/verify/privacy/cancel coverage. The worker is 34,523/13,687 B raw/gzip and stays outside shell/mandatory Workbox; no upstream implementation code was copied. |
| Existing-route interoperability and shell recovery | DONE | Integer Base has exact signed bases 2–64 with common notation and correct <=36 versus 37–64 case semantics; 17 structured-text/authoring paths share bounded downloads over existing results; Home has canonical URL-state filtering and keyboard favorite reorder with focus transfer. Native drag/drop removes two runtime packages and cuts shell/PWA by 107,897 B raw / 38,855 B gzip. No upstream implementation code was used. |
| DevOps/JSON/crypto authoring wave | DONE | Existing routes now provide bounded JSON/YAML/TOML subtree-to-`.env`, deterministic RFC 6902 `add`/`remove`/`replace` patches with RFC 6901 escaping and preserved numeric lexemes, explicit UTF-8/hex/canonical-Base64 HMAC keys with RFC 4231 coverage, a precise legacy-encryption format boundary, and bounded Token batches/custom-denied alphabets. Sensitive content is ephemeral, only harmless Token preferences persist, and no runtime dependency or upstream code was added. |
| Security/search/JSON interoperability | DONE | Added compatible worker-owned Ansible Vault 1.1/labelled 1.2 AES256 and Apache bcrypt htpasswd workflows with strict bounds, generic authentication failure, and ephemeral secrets; Command Palette now has deterministic exact-first ranking plus accessible Show all; JSON Repair & Query adds explicit strict non-destructive escaped-JSON import. No runtime dependency or upstream implementation code was added. |
| Tabular/Wi-Fi/permissions interoperability | DONE | Added one lazy bounded CSV/TSV Tabular Data Inspector with file/paste input, one worker parse, profile/preview, explicit text versus conservative inference, normalized export and formula-prefix protection. Wi-Fi QR now exposes scanner-compatible WPA3/transition intent plus exact raw-payload copy without claiming SAE enforcement; Chmod parses octal/symbolic forms, special bits, and creation umask. No runtime dependency or upstream code was added. |
| JWT/HOTP, SPDX, and OKLCH wave | DONE | Existing JWT and OTP routes provide bounded explicit HMAC JWT author/sign/verify and exact RFC 4226 HOTP workflows; the later JWK/JWKS slice now also supplies local public-key RSA/ECDSA/EdDSA verification. The lazy SPDX route provides a disclosed 17-license versioned common subset and conservative non-legal guidance; Color Converter adds standard OKLCH plus explicit sRGB gamut mapping. Sensitive content is ephemeral; full SPDX expression/Local MINDE claims remain excluded. |
| Bounded storage/privacy/crypto wave | DONE | Added exact BigInt-rational RAID 0/1/5/6/10 capacity planning, lossless signature-verified JPEG/PNG/WebP metadata removal in a 32-MiB disposable worker, and versioned ITAE-v1 AES-256-GCM text/file envelopes with PBKDF2, authenticated headers, encrypted file metadata, strict limits, generic authentication failure, and ephemeral secrets. No runtime dependency or upstream implementation code was added. |
| Priority A seven-feature batch | DONE | Added deterministic Faker-compatible mock data, exact BigInt IPv6/CIDR, bidirectional Docker conversion, bounded sensitive-data/HAR sanitization, IANA timezone/date duration, worker-backed Cron next runs, and local barcode generation/native-reader integration. Five routes are lazy, two existing routes are enhanced, no runtime dependency was added, sensitive content is session-only, and unit/E2E/bundle gates pass. |
| Priority A second feature wave | DONE | Added lazy CLI Command Editor, DevOps Configuration Workspace, JSON Repair & Query, and Certificate/CSR Inspector routes, and expanded the existing UUID route into an identifier workspace. Commands/configuration are never executed, structured work is bounded and worker-owned where appropriate, private-key PEM is rejected, sensitive input remains ephemeral, and no runtime dependency or upstream implementation code was added. |
| Priority A closure wave | DONE | Added lazy Local File Inspector, JSON Schema & Code Generator, Ed25519 & SSH, and QR Decoder & OTP Import routes, and expanded URL Encoder into a safety/authoring workspace. File/JSON/key work is bounded and worker-owned; native QR/camera and Ed25519 are capability-disclosed; secrets and files remain ephemeral; no runtime dependency or upstream implementation code was added. |
| Bounded offline utility wave | DONE | Added lazy Developer Text, Network Calculation, SAML/Enterprise Timestamp, and Favicon/App Icon routes. Non-diff layouts stay vertical; text/SAML work is bounded and worker-owned; IPv4/IPv6 uses exact BigInt; image/assertion inputs are ephemeral; SAML is explicitly non-verifying; no runtime dependency or upstream implementation code was added. |
| Priority B browser-native wave | DONE | Added seven lazy local routes: secure passphrases, native GZIP, RFC 3492 IDN safety, exact data/transfer units, worker-owned Markdown tables, IEEE-754 inspection, and Conventional Commit authoring. Inputs and sensitive outputs remain ephemeral; amplifying work is explicit and bounded; limitations are disclosed; no runtime dependency or upstream implementation code was added. |
| Priority B local utility wave | DONE | Added lazy List Comparison, Unicode/GSM-7 Inspector, Date/ISO Week/iCalendar Utilities, and HTTP Request & Code Builder routes. List work is complexity-bounded and worker-owned; Unicode/SMS and calendar limitations are explicit; HTTP/cURL is strict non-executing authoring with default recognized-secret redaction. No runtime dependency or upstream implementation code was added. |
| PWA uncached-route recovery | DONE | Strict lazy-tool/network-error classification, effective-offline probing, query-safe in-memory state, prior-route hiding, and accessible actions pass 14/14 unit/component checks and a production-Workbox E2E. Retry first probes the app origin, then starts a fresh query-free document because failed native imports are sticky and query/hash must not reach access logs. |
| Dev freshness/cache isolation | DONE | Source-served dev is fixed to `127.0.0.1:8091`, preview to `127.0.0.1:5050`, both with strict ports. Dev responses use `no-store` plus an explicit mode header; `dev:fresh` forces Vite dependency optimization, while dev runtime unregisters only the root IT Tools Workbox worker and removes only application cache prefixes. Worker-only and lazy-route dependencies are explicitly included/excluded from Vite 4 optimization; `jsonc-parser`, Monaco, Mermaid, `hyparquet`, `saxen`, `pkijs`, worker-transitive `js-base64`, and `hash-wasm` are covered by a source-contract test, and the current fresh traversal covers 131 routes without optimizer reload. Source-dev acceptance must use `pnpm test:e2e:dev`; bare Playwright uses preview port 5050 by configuration. |
| M1 container delivery | IN PROGRESS | Rootless/read-only/default-and-arbitrary-UID contracts pass with alternate internal port, strict static 404, gzip/cache/headers, and privacy-safe access logs. The CVE/remediation track is explicitly deferred; reverse-proxy/subpath acceptance remains. |
| Secure randomness | DONE | Web Crypto rejection sampling, alphabet fixes, length bounds, Unicode/boundary tests, and non-persistence browser checks are implemented. `.ai/RANDOMNESS.md` classifies every caller; UUID v1 now seeds its multicast node and 14-bit clock sequence with Web Crypto, while remaining pseudo-random callers are presentation-only. |
| Text Diff lifecycle/privacy | DONE | Minimal Monaco editor import, real worker, multi-owner environment restoration, complete disposal, default-off bounded/versioned persistence, quota handling, and explicit clear are covered. Ten warmed route cycles retain +2.46 MiB and zero workers (`<5 MiB` budget). |
| Text Diff payload | DONE | The worker-backed CodeMirror spike is 363,669 B raw / 118,937 B gzip and passes its 1 MiB worker/lifecycle/Long Task probe, but public MergeView result publication rebuilds the view and loses focus, undo, and history. Repaired Monaco remains the accepted implementation and its 646,976 B gzip closure is a documented parity-driven exception. |
| MAC/OUI route payload | DONE | The accepted privacy-safe design generates one compact fixed-URL worker from 34,503 pinned records. Its full closure is 1,937,384 B / 770,467 B, 27.6% less gzip than the original route. Input-selected buckets were rejected because filenames leaked MAC prefixes; exact coverage plus no input-derived network URLs is now an accepted product exception. |
| YAML Prettify responsiveness | DONE | One terminate-and-replace worker parses each document exactly once with 2 MiB input, 4 MiB output, depth/node/alias, five-second, job-ID, cancellation, stale-result, disposal, and exact worker-side UTF-8 bounds. A conservative graph projection rejects scalar-alias, deep-sequence, escaped-line, and boundary-space amplification before serialization while preserving the 100,000-node capacity. Large integers use `BigInt`; 31 focused checks and the final isolated production 1 MiB fixture pass at 203 ms format-ready with 0.0 ms longest Long Task. |
| JSON lossless/worker slice | DONE | Strict JSON Prettify uses one bounded worker AST, preserves integer/decimal/exponent/negative-zero lexemes, rejects duplicate decoded keys, and sorts decoded keys without touching value tokens. JSON5 is explicitly non-lossless and uses an incremental bounded writer. Thirty-three focused checks and the final production 1 MiB fixture pass at 404 ms cold-route / 197 ms format-ready with 0.0 ms longest Long Task. |
| Shared one-shot worker transport | DONE | JSON/YAML/File Hash use the smallest common typed-envelope/job-ID/timeout/cancel/stale/dispose transport without changing literal route-owned worker URLs. `messageerror` settles deterministically and clears handlers/timers; File Hash adds validated progress and exact-key envelopes. Persistent OUI indexing is not forced into the one-shot abstraction; envelope-first stale filtering and broader parser/output/download policy remain incremental. |
| Worker protocol correctness | DONE | Legacy JSON/YAML/Bcrypt guards reject arrays; Bcrypt preserves valid correlation IDs; OUI errors are static, bounded, Unicode/control-sanitized on both boundaries and malformed messages force replacement. File Hash rejects every extra request/progress/result/digest/error field, preventing future filename/byte leakage across the worker boundary. |
| Shared refreshable tasks | DONE | Seven remaining callers use explicit dependency sources and single-execution refresh with AbortSignal/stale-result/scope guards. RSA left this abstraction for an explicit route-owned Web Crypto worker that is physically terminated on cancel, replacement, timeout, and unmount. |
| RSA Key Pair Generator | DONE | Fixed 2,048/3,072/4,096-bit presets replace the arbitrary spinner; generation is explicit, previous output survives pending/cancel/error, and strict Web Crypto worker envelopes enforce a 30-second deadline plus physical termination. Direct `node-forge` and its type package are removed. The additional route closure is 60,832 B raw / 24,700 B gzip, down from 450,149 B / 132,813 B; the owned worker is 2,498 B / 1,151 B. Focused 22/22 RSA unit/component plus 2/2 Vite-dev Chromium flows pass. |
| Ordered QR generation | DONE | QR and Wi-Fi QR wait 150 ms after edits, accept only the latest snapshot, reject payloads above 4,096 UTF-8 bytes, clear incomplete Wi-Fi results immediately, disable stale downloads, and invalidate pending timers/promises on scope disposal. Focused 6/6 composable and 2/2 Chromium flows pass. |
| HTML WYSIWYG lifecycle | DONE | Prettier runs in a terminate-and-replace worker after a 250 ms pause below 64 KiB; larger documents require an explicit action, input/output are bounded at 1/2 MiB, and an eight-second timeout plus cancellation/stale/unmount guards apply. Twelve focused unit/component and 2/2 Chromium flows pass. The main route chunk fell to 287,982 B / 86,389 B gzip; its worker is 212,679 B / 70,280 B gzip, with an accepted 551,423 B / 176,574 B additional closure. |
| JSON Diff bounded comparison | DONE | Each explicit worker request parses both JSON5 documents once under 1 MiB-per-side, depth-128, 100,000-total-input/output-node, eight-second, and 250,000-LCS-cell limits. Unique `id`/`key`/`name` object arrays and primitive sequences align without cascading updates; other arrays report positional fallback. The reproduced depth-80 case remains linear, nested containers mount lazily, wide trees render in 200-row batches, and the 827,203-byte production fixture passes five repeats with heartbeat and no observed Long Task. |
| Bounded text-tool workers | DONE | Math, SQL, XML, Markdown, and Text Statistics share debounce/explicit action/status/lifecycle behavior while retaining route-local workers. SQL now publishes a 16 KiB preview while full output remains available for Copy and Blob download; its 716,800-byte production result passes with 0.0 ms observed Long Task. |
| Structured converter workers | DONE | JSON-to-TOML/YAML, YAML-to-JSON/TOML, TOML-to-JSON/YAML, and XML-to-JSON/JSON-to-XML use one shared vertical bounded-transformer contract. Three source-family workers plus one XML-family worker parse each source once under 64 KiB live / 1 MiB hard input / 2 MiB output / eight-second limits with terminate/replace, stale-result, previous-result, and disposal behavior. Converter number semantics remain library-specific rather than inheriting the strict Prettify lossless claim. |
| Docker Compose ↔ Docker Run worker | DONE | The original composerize path and the new reverse YAML parser share one strict route-owned worker. It auto-runs below 16 KiB, requires an explicit action up to 256 KiB, caps output at 512 KiB and messages at 100 x 4 KiB, terminates after four seconds, retains prior output, and disposes on replacement/cancel/unmount. Reverse conversion covers shell quoting, environment, ports, volumes, entrypoint and command with explicit unsupported-field guidance; downloads use short-lived Blob URLs. |
| Hash Text worker | DONE | All eight existing CryptoJS digests move from one reactive main-thread computation into one terminate-and-replace worker. Empty-string and Bin/Hex/Base64/Base64url semantics are preserved; work auto-runs only below 16 KiB, requires an explicit action up to the 1 MiB hard input bound, caps the exact-key result envelope at 8 KiB, times out after five seconds, retains prior output, and physically terminates on replacement/cancel/unmount. MD5 and SHA-1 remain visibly marked as legacy. |
| Remaining formatter workers | DONE | JSON-to-CSV parses JSON5 once in a bounded worker and limits CSV amplification; JSON Minify reuses the structured JSON worker; measured List Converter work runs in its own bounded worker. Production fixtures near 1 MiB pass with heartbeat and 0.0 ms observed Long Task, and the zero-caller `FormatTransformer` has been deleted. |
| Initial-shell dependency slice | DONE | Seven shell-critical Lodash imports were replaced with native equivalents. The apples-to-apples shell comparison is 278,135 B gzip before versus 277,702 B after (-433 B); raw grows 343 B, so the accepted claim is deliberately limited to gzip transfer. |
| Shell/Home lifecycle | DONE | Persistent `BaseLayout` removes the detached-layout leak: ten Home/tool cycles improved from +188.40 MiB to +0.86 MiB; DOM and listener counts are stable. |
| ASCII font delivery | DONE | Removed the `unpkg.com` runtime dependency. All 289 Figlet fonts are emitted as versioned same-origin assets, excluded from install-time precache, cached on demand, and covered by browser/container smoke. |
| Confirmed product fixes — slice 1 | DONE | Base64 File, Temperature, URL Parser, Command Palette, CSV, Text Statistics, and Docker Compose output pass 65 focused tests across nine files plus scoped lint. |
| Responsiveness/lifecycle — slice 2 | DONE | Bcrypt and Regex use bounded cancellable workers; Regex preflights sample expansion and preserves zero-width matches; mobile navigation separates transient/persisted state and is inert/focus-safe; Camera has byte/time/raw-allocation/aggregate bounds plus single-flight capture; shared buttons have real disabled semantics. DOM-dependent Regex SVG remains an explicit bounded main-thread residual. |
| Shared large-output rendering | DONE | Above 100,000 UTF-8 bytes, highlighted output becomes one zero-descendant readonly `<textarea>` with a UTF-8-safe 100,000-byte preview; Copy retains the complete bounded output in memory. JSON/YAML 1 MiB fixtures pass the `<50 ms` Long Task gate. |
| Emoji bounded rendering | DONE | One responsive row window mounts only the visible slice plus one overscan row and preserves group headers, total geometry, Fuse relevance, ARIA position/set metadata, and keyboard navigation across unmounted items. Search is a bounded cancellable route-owned worker; repeated production 4x-CPU flows pass the strict `<50 ms` Long Task gate. |
| Cross-tool persistence/privacy | IN PROGRESS | The current storage inventory is classified in `.ai/PERSISTENCE.md`: thirteen legacy content/network-input keys are session-only, secrets are ephemeral, Text Diff is the sole bounded opt-in, Regex edits stay out of URL/history, analytics strips query/hash/referrer credentials, and About provides disclosure plus a managed-key-only global reset. A common preference-storage denial boundary remains. |
| M5 first catalog feature | DONE | NanoID Generator is the first delivered bounded catalog feature: Web Crypto rejection sampling, no content persistence/network transport, explicit generation, NFC Unicode custom-alphabet validation, entropy/collision guidance, 100,000-symbol / 512-KiB limits, copy/download/clear, lazy route, and bundle/browser gates. Its additional closure is 23,225 B raw / 8,916 B gzip; the second feature is also delivered. |
| M5 second catalog feature | DONE | JSON Schema Validator supports Draft 7/2019-09/2020-12 in a route-owned bounded worker with fragment-local references, source-aware sanitized errors, explicit actions, session-only privacy, safe equality for prototype-shaped data, and executable performance/PWA budgets. `allErrors` is additionally bounded by 20,000 projected schema/instance node pairs. Focused 121/121 and Chromium 3/3 pass; issues #368/#801 were requirements research only. |
| M5 third catalog feature | DONE | Local File Hash is delivered under `.ai/experiments/FILE_HASH_DESIGN.md`: one fixed-4-MiB-window worker pass supports SHA-256/384/512, SHA3-256, BLAKE3-256, SHA-1, and MD5; the two legacy algorithms are visibly warned. Exact-key protocol, progress/cancel/replacement, no application whole-file buffer, session-only file references, safe filename display, online/offline PWA reuse, sampled peak memory, and executable route/worker budgets remain in force. Issues #528 and PRs #535/#1141 remained requirements-only research. |
| Worker transport hardening — compatible increment | DONE | Deterministic `messageerror` settlement, strict non-array legacy record guards, bounded sanitized OUI error text, and File Hash exact-key/progress lifecycle coverage pass. Envelope-first stale filtering and output-byte-metadata trust remain explicit follow-ups instead of being overstated as complete. |

## Verified checkpoints

Current XLSX integrated evidence (2026-08-16): lint and both typecheck
projects pass; the full unit suite passes 1417/1417 across 245 files; the fresh
production build transforms 24,163 modules; all 404 artifact-budget checks pass;
the targeted source-dev scenarios pass 2/2 and all 129 registry routes pass in
5.2 minutes. The build emits 642 files / 18,151,938 B raw / 5,303,777 B gzip;
shell including document is 844,594 B / 247,458 B gzip and mandatory Workbox is
9 files / 898,442 B / 297,820 B gzip. The sequential all-route source-dev smoke
completes without runtime/chunk errors, responsive overflow, unnamed visible
controls, or optimizer restart.

- `pnpm install --frozen-lockfile`: passed against the pinned pnpm 9.11.0 lock-resolved graph.
- `pnpm lint`: final integrated run passed with zero errors/warnings.
- `pnpm typecheck`: final integrated application/test and Vite-config projects passed.
- `pnpm exec vitest run --environment jsdom`: current integrated run passed 1417/1417 across 245 files; the focused XLSX service/ZIP/protocol suite passes 35/35.
- `pnpm test:build-stats`: schema-v4 16/16 passed, including worker discovery/ownership, unrelated-worker exclusion, malformed-reference failure, manifest graph, stable identity, subpath handling, strict route/worker budgets, and required Workbox membership.
- `pnpm build`: final integrated production build passed; OUI freshness and dual typecheck run first and 24,163 modules were transformed. The earlier exact-Node-24.18.0 Docker build remains the exact-baseline environment check.
- Chromium matrix: a combined run passed 175/176 functional/performance cases, with only NanoID's timing observation affected by external CPU contention; the corrected NanoID file then passed 4/4. The bounded-worker performance file passes 11/11, and the File Hash/reactive group passes all 5 relevant cases in isolation. No payload, timeout, or `<50 ms` threshold was weakened. A later combined run was stopped after `ps` showed unrelated Zed, VS Code, Zen, and Orca processes consuming roughly 29–75% CPU each and stretching otherwise-zero Long Task samples to 50–94 ms; it is not recorded as application evidence.
- Chromium all-tool route smoke: all 129 registry-backed routes pass in 5.2 minutes against the canonical fresh Vite dev server without runtime/chunk errors, horizontal overflow, unnamed visible controls, Monaco worker fallback warnings, or optimizer reload. XLSX passes 2/2 on the same source-served origin: bounded DEFLATE workbook inspect/preview, exact large scalar lexemes, cached-formula disclosure, JSON/CSV actions, cancellation/restart/Clear, privacy, and wide vertical desktop layout.
- ASCII same-origin E2E: passed; no external font request is made.
- Text Diff forced-GC gate: 20,423,472 B -> 23,001,392 B after ten warmed cycles, +2,577,920 B (+2.46 MiB), zero remaining workers.
- Home forced-GC gate: before fix +188.40 MiB with DOM 9,064 -> 74,694 and listeners 1,939 -> 14,988; after fix 18,336,088 B -> 19,242,160 B, +906,072 B (+0.86 MiB), DOM 5,036 -> 5,037, listeners 807 -> 809.
- Rootless nginx smoke: default UID 101 and arbitrary UID 12345 pass with root-owned non-writable artifact/config, read-only rootfs, tmpfs `/tmp`, all capabilities dropped, and configurable internal port.
- HTTP delivery smoke: health/SPA fallback, strict missing static/Workbox `404`, local Figlet font, HTML/SW/manifest policies, immutable assets, gzip, security headers, and query/referrer-safe access logs pass.
- Workflow syntax: all GitHub workflow YAML parses; entrypoint/container scripts pass `sh -n`; `git diff --check` passes.
- Correctness slice 1: 65/65 focused tests pass across Base64 File, Temperature, URL Parser, Command Palette, CSV, Text Statistics, and Docker Compose; scoped ESLint and `git diff --check` pass.
- Regex responsiveness: 36/36 focused unit/component checks across five files and 4/4 Chromium flows pass for optional/zero-width captures, pre-allocation sample bounds, limits/protocol validation, explicit incoming-query/session-only behavior, catastrophic timeout with heartbeat, stale cancellation, route leave, and tool-to-tool unmount.
- Shared `TextareaCopyable`: 9/9 component checks plus 16/16 UTF-8 helper checks pass; the 1 MiB JSON/YAML Chromium fixtures render one UTF-8-safe 100,000-byte readonly preview with zero descendants, and JSON verifies Copy receives the complete output.
- Build budgets: 16/16 infrastructure tests and 404/404 current production-artifact checks pass; CI/release enforce byte/count ceilings, owned-worker closures, exact reviewed Mermaid catalog exceptions, independent reviewed worker ceilings including Parquet and XLSX, and mandatory Workbox shell/static membership.
- Current converter focus: XML-to-JSON/JSON-to-XML and Docker Run-to-Compose pass 7/7 lifecycle flows against the confirmed Vite dev server on port 8091. The production responsiveness suite passes 7/7 for SQL, XML Formatter, XML-to-JSON, Markdown, Text Statistics, JSON Diff, and Docker conversion with no observed Long Task.
- Bcrypt responsiveness: 19/19 original worker/protocol unit checks and 4/4 Chromium flows pass; current protocol hardening is included in the 46/46 Bcrypt/OUI set. The lazy route is 11,427 B raw / 4,160 B gzip and its worker is 25,436 B raw / 11,394 B gzip.
- Camera Recorder lifecycle: 18/18 model/composable/component checks pass with deterministic Blob URL ownership, 1-second chunks, 5-minute/64-MiB recording, single-flight screenshots, a pre-canvas 16,777,216-pixel/64-MiB raw bound, a 16-MiB encoded bound, and a 128-MiB aggregate bound.
- Shared buttons: 4/4 component/router checks pass for non-submitting native defaults, explicit submit/reset, physical href removal plus link semantics while disabled, and enabled object-route navigation.
- URL Encoder standards: 19/19 model regressions pass across component, RFC3986, RFC5987 value-body, and form-urlencoded modes.
- UnoCSS native `size`: 15/15 input/config regressions pass with the original Attributify ignore defaults preserved.
- Sensitive-content migration checkpoint: the current 816/816 full unit run, 15/15 focused storage checks, and 2/2 Chromium privacy flows pass after making thirteen content/network-input keys ephemeral and cleaning legacy values.
- Text Diff 1 MiB interaction: two exact 1,048,576-byte models reach ready diff state in 725 ms; a clear UI action takes 45 ms, the real worker remains active, storage remains default-off, and the focused production-preview suite passes.
- Refreshable task abstraction: 9/9 new invocation/cancellation tests and 38/38 related unit checks cover the original eight-caller migration; seven remain on that abstraction, while RSA now has its explicit physically cancellable worker task.
- Privacy/reset UX: 4/4 focused tests pass; clearing managed settings/content preserves unrelated same-origin keys and reports denied mutations.
- Mobile menu: 4/4 layout regressions, 2/2 style-store checks, and 1/1 Chromium focus flow pass for immediate small-screen collapse, route-close, Escape, focus restoration, inert desktop/mobile hidden states, and a preserved persisted desktop preference.
- Emoji bounded rendering/search: focused unit/component/browser flows pass; one responsive row window with one-row overscan preserves catalog/search ranking, group semantics, ARIA positions, and keyboard movement across unmounted items. Bounded worker search repeatedly passes the strict 4x-CPU `<50 ms` Long Task gate.
- Analytics/URL privacy: 3/3 plugin checks cover path-only event/pageview URLs and sanitized referrers; 2/2 Regex component checks cover explicit incoming query import without editor write-back.
- PWA demand caching: 10/10 policy checks and the generic plus File Hash Chromium offline flows pass. The final precache is nine entries / 961,155 B raw / 328,079 B gzip; Random Port proves generic lazy-route caching, while File Hash separately proves both its route and worker are runtime-cached and can compute again after HTTP-cache clear/offline reload.
- PWA uncached-route recovery: 14/14 classifier/component checks and 1/1 production-Workbox flow pass; an uncached target replaces sensitive prior DOM with recovery UI, an origin probe prevents navigation into the browser's own offline page, a fresh query-free document clears the sticky import failure after reconnect, and the target enters the lazy cache.
- YAML Prettify: 31/31 handler/protocol/client/model/component checks, 10/10 repeated isolated Long Task runs, and the final 1/1 production flow pass; one bounded worker parse preserves large integers, exact UTF-8 accounting stays off-main-thread, pre-serialization projection blocks four output-amplification classes, format-ready is 203 ms, and no current-interaction Long Task is observed. Chromium garbage from prior discarded contexts is collected before the measurement window, without excluding any formatting allocation.
- JSON Prettify: 33/33 handler/protocol/client/model/component checks and 1/1 1 MiB Chromium flow pass; strict formatting is lossless for numeric lexemes, full Copy is exact, cold route is 404 ms, format-ready is 197 ms, and no Long Task is observed.
- NanoID Generator: 53/53 service/component/download-helper checks and 4/4 Chromium flows pass; maximum 100 x 1,000 generation is 144 ms with no Long Task, clipboard/download are exact, and output/alphabet remain absent from URL, requests, and storage.
- MAC/OUI privacy and lifecycle: generator 4/4, data/protocol/client/service 28/28, and Chromium 2/2 pass; one prefix-independent worker request is reused offline, retry uses the exact same URL, no tested prefix enters a URL/body, and SPA leave disposes the worker.
- UUID v1 randomness: 8/8 service regressions pass with Web Crypto seeding, multicast-node semantics, a 14-bit clock sequence, and no `Math.random` path.
- Shared one-shot worker transport: utility/JSON/YAML/File Hash regressions pass with terminate-and-replace cancellation, timeout, `messageerror`, stale-message, progress-decode failure, and disposal coverage while public clients remain stable.
- Bcrypt/OUI protocol correctness: strict record guards, bounded sanitized errors, preserved valid correlation IDs, malformed-envelope classification, and worker invalidation pass in the full/focused transport suites.
- JSON Schema Validator: 121/121 model/protocol/handler/client/component regressions and 3/3 Chromium flows pass; the final 1 MiB production fixture is 246 ms cold / 205 ms result-ready / 0.0 ms longest observed Long Task, branch-heavy `allErrors` is node-pair bounded, and content remains absent from URL, storage, and requests.
- File Hash: focused transport/tool/upload 107/107 and production Chromium core/privacy/copy/reselection 3/3, large-file 1/1, and PWA offline 1/1 pass. The 256 MiB/all-three run is 9,999 ms, 0.0 ms longest observed Long Task, +1,922,904 B peak page heap, +91,242,496 B peak sampled Chromium-process RSS, and +260,696 B retained page heap; replacement/cancel occur after nonzero progress and leave zero workers.

## Current production measurements

The audit baseline used the original CDN-hosted Figlet design. The current
artifact deliberately adds 289 versioned local font files (2,888,669 B raw /
478,400 B theoretical gzip), but excludes them from the mandatory Workbox
precache and caches at most 32 requested fonts at runtime.

| Metric | Audit baseline | Current schema-v4 build | Change |
|---|---:|---:|---:|
| Total dist files | 286 | 511 | +225 (+78.7%; includes 289 local fonts) |
| Total dist raw | 12,816,672 B | 13,329,332 B | +512,660 B (+4.0%) |
| Total dist gzip | 3,893,533 B | 3,812,444 B | -81,089 B (-2.1%) |
| Shell gzip incl. document | 273,513 B | 276,963 B | +3,450 B (+1.3%) |
| Workbox entries | 270 | 9 | -261 (-96.7%) |
| Workbox raw | 6,121,476 B | 959,654 B | -5,161,822 B (-84.3%) |
| Workbox gzip | 1,911,520 B | 327,923 B | -1,583,597 B (-82.8%) |
| Text Diff JS raw | 3,161,296 B | 2,206,864 B | -954,432 B (-30.2%) |
| Text Diff JS gzip | 801,738 B | 570,786 B | -230,952 B (-28.8%) |
| Text Diff additional route + worker gzip | not previously separated | 646,976 B | target remains `<350 kB` |
| MAC Lookup additional route + worker raw | 3,350,554 B | 1,937,384 B | -1,413,170 B (-42.2%) |
| MAC Lookup additional route + worker gzip | 1,064,267 B | 770,467 B | -293,800 B (-27.6%) |
| NanoID additional route raw / gzip | not present | 23,225 B / 8,916 B | first local catalog feature |
| JSON Prettify additional route + worker raw / gzip | not previously worker-aware | 208,871 B / 70,845 B | worker is 46,309 B / 13,989 B |
| YAML Prettify additional route + worker raw / gzip | not previously worker-aware | 261,438 B / 87,579 B | worker is 99,735 B / 31,033 B |
| JSON Schema Validator additional route + worker raw / gzip | not present | 176,585 B / 53,355 B | worker is 161,471 B / 47,125 B; both gates pass |
| JSON Schema registry shell increment | prior checkpoint 276,295 B gzip | 276,645 B gzip | +350 B (+0.1%) accepted for route metadata/icon; Ajv stays lazy and worker-owned |
| File Hash additional route + worker raw / gzip | not present | 56,994 B / 22,189 B | worker is 13,583 B / 6,298 B; independent route and worker gates pass |
| File Hash registry shell increment | prior checkpoint 276,645 B gzip | 276,963 B gzip | +318 B (+0.1%) accepted for route metadata/icon; hashing stays lazy and worker-owned |
| Emoji additional route raw | 386,059 B | 272,834 B | -113,225 B (-29.3%) |
| Emoji additional route gzip | 63,420 B | 32,488 B | -30,932 B (-48.8%) |

## Completed implementation units

- Restored a zero-warning lint baseline and canonical application/test/Vite type checking.
- Made unit tests one-shot by default and made production build invoke canonical typecheck.
- Aligned Node 24.18.0/pnpm 9.11.0 across engines, `.nvmrc`, CI, Docker, and documentation; pinned first-party/third-party actions by SHA and disabled persisted checkout credentials.
- Added frozen installs, corrected Playwright cache identity, BuildKit fetch/cache, strict SemVer release validation, immutable version tags, draft-before-push ordering, SBOM, and provenance declarations.
- Moved build/type-only packages out of runtime dependencies without importing an upstream lockfile.
- Added deterministic schema-v4 build/PWA statistics with compact closure membership digests and fail-closed ownership of literal local workers in route closures.
- Added secure unbiased browser randomness, restored `N`/`n`, removed duplicate symbols, bounded untrusted token length, and added non-persistence coverage.
- Reworked Text Diff worker ownership, lifecycle disposal, persistence privacy, quota recovery, and forced-GC regression gates.
- Fixed the application-shell detached layout leak and added a cross-route heap/DOM/listener gate.
- Removed ASCII Art's third-party CDN dependency using versioned same-origin on-demand font delivery.
- Added all-89-route smoke coverage and narrow worker/chunk/runtime warning gates.
- Added pinned rootless container delivery, configurable port, read-only operation, healthcheck, gzip/cache/security headers, strict static routing, and automated default/arbitrary-UID smoke.
- Corrected release image namespaces from upstream `corentinth` to the local fork `64mb`.
- Fixed Base64 MIME/raw-data URIs, all-scale Temperature bounds/rounding, ordered URL query duplicates/fragments, guarded Command Palette keyboard activation, standard CSV quoting, single-pass Text Statistics, and obsolete Compose output with focused regressions.
- Fixed Regex optional captures and shared output copy-placement/late-height tracking.
- Added machine-readable build budget schema, default route ceilings, documented heavy-route exceptions, independent reviewed worker-asset ceilings, and CI/release enforcement.
- Replaced route-wide Workbox precaching with a nine-entry shell policy (including the fail-closed discovered Workbox client runtime), bounded runtime caching for opened hashed routes, required-entry checks, product-target byte ceilings, and a clean-HTTP-cache offline reload fixture.
- Replaced reactive Bcrypt work with explicit bounded worker actions; bounded Camera Recorder duration/chunks/raw canvas/encoded output/item and aggregate bytes with single-flight capture; added four URL encoding modes; restored native input `size` and shared disabled-button contracts.
- Replaced getter-as-watch refresh logic with explicit dependencies, one execution per refresh, async stale guards, AbortSignal cancellation, and scope disposal across eight generators.
- Removed default raw-content persistence from JSON/YAML viewers, JSON Diff, HTML WYSIWYG, Benchmark Builder, Case Converter, Regex, IPv4 converter/range/subnet, and MAC prefix while preserving harmless settings; explicit incoming Regex query import remains, but editor changes never write back to URL/history.
- Sanitized Plausible custom events and automatic pageviews to path-only URLs and stripped referrer credentials/query/hash before transport.
- Added an About privacy boundary and global reset that removes only the audited managed-key registry, leaving other same-origin application data untouched.
- Moved Regex matching and RandExp into independent terminate-and-replace workers with 1.2-second deadlines, limits, cancellation/stale guards, route disposal, and catastrophic-heartbeat coverage; added pre-allocation AST/backreference bounds and single-flight DOM-dependent SVG.
- Added one zero-descendant readonly `<textarea>` preview above 100,000 UTF-8 bytes and 1 MiB JSON/YAML browser regressions that preserve full copy access without rendering the complete result into the DOM.
- Paged Emoji Picker 60 cards at a time, lazy-loaded keyword metadata, preserved complete ZWJ/flag sequences and Fuse result order in one grid, and replaced clickable text with keyboard-native copy buttons.
- Closed the small-screen menu after route changes and Escape without changing the persisted desktop collapse preference; hidden navigation is inert at both breakpoints and focus returns to the labelled toggle.
- Replaced YAML Prettify's double parse/reactive main-thread path with one bounded parse-once worker, `BigInt` integers, explicit formatting above 100 kB, worker-side exact UTF-8 bounds, cancellation/stale guards, and a 1 MiB `<50 ms` Long Task browser gate.
- Added strict lossless JSON Prettify on one bounded worker AST, decoded-key duplicate/sort rules, an incremental bounded JSON5 writer, worker-side exact UTF-8 accounting, complete-output Copy, and a 1 MiB `<50 ms` Long Task browser gate.
- Delivered the dependency-free NanoID catalog feature with Web Crypto rejection sampling, Unicode-aware custom alphabets, exact/approximated collision guidance, hard batch/output limits, copy/download/clear privacy coverage, and a measured 8,916 B gzip additional route.
- Added a privacy-safe offline lazy-route state that hides the old tool, distinguishes origin unavailability from ordinary code errors, and recovers sticky failed imports through a query-free fresh document.
- Replaced the OUI monolith with reproducibly generated compact data in a fixed-URL route worker; rejected the smaller prefix-bucket prototype because input-derived filenames would enter access logs.
- Completed the Text Diff editor payload comparison and secure-randomness inventory; retained repaired Monaco pending a worker-backed CodeMirror spike and removed UUID v1's pseudo-random seed path.
- Delivered the bounded three-draft JSON Schema Validator with local references, session-only privacy, source-aware sanitized errors, safe prototype-shaped equality, explicit actions, and measured worker/route/PWA budgets.
- Extracted the proven JSON/YAML terminate-and-replace worker-task transport and closed Bcrypt/OUI malformed-envelope and error-sanitization correctness gaps with focused regressions.
- Delivered File Hash with one fixed-window SHA-256/384/512 worker pass, explicit progress/cancel/replacement, exact-key wire boundaries, session-only privacy, grapheme-aware safe filename display, clipboard/empty/same-file coverage, and 256 MiB responsiveness plus peak-memory evidence.
- Repaired the shared file-upload control so native same-file selection and drop-state reset are deterministic, and hardened shared transport `messageerror`, legacy non-array records, and OUI error-text boundaries without forcing persistent workers into the one-shot abstraction.
- Added a separately reviewed File Hash worker budget and an online-to-offline production Workbox flow proving the route and worker stay demand-loaded yet reusable offline.

## Active risks and deliberately deferred work

- The final source-host measurements used Node 24.15.0 and emitted an engine warning; this is historical benchmark context, not a portable requirement. The selected and aligned baseline is Node 24.18.0, and the exact-baseline build is covered by Docker. A resumed machine should use `.nvmrc`.
- `@types/node` remains 18.x. The current lock explicitly overrides `@unhead/vue` to compatible `@vueuse/shared@10.3.0`; Mermaid delivery also pins patched `lodash@4.18.1` and overrides shared `lodash-es@4.18.1`, removing three Lodash advisories. The current production audit still reports 52 unrelated advisory records across 57 severity-counted findings; they require the separately controlled remediation track. `hyparquet` has no advisory path.
- Text Diff meets its heap/worker budget but not the original route payload target. The worker-backed CodeMirror spike passed size and isolation gates but failed focus/undo/history parity, so Monaco is the accepted documented exception until an editor can meet both interaction and payload requirements.
- Local Figlet assets increase static artifact file count/size but remain excluded from the nine-entry mandatory precache; on-demand runtime caching and a separate same-origin browser/container fixture pass.
- Regex SVG remains an explicit bounded main-thread task because `@regexper/render` requires the DOM; selecting a DOM-free or isolated renderer is separate work.
- Emoji uses true DOM windowing and its bounded worker search passes the strict 4x-CPU `<50 ms` Long Task profile. Refreshing the two Unicode datasets remains separate catalog-data maintenance.
- PWA demand caching, uncached-route recovery, stale dev-cache cleanup, and worker-dependency optimizer stability pass. User-facing update notification/rollback UX remains a separate feature.
- The privacy-safe exact-coverage OUI worker is 27.6% smaller gzip than the original route and is an accepted exception to the earlier `<250 kB` target. Reopening it requires a product decision to reduce coverage or change the no-input-derived-URL privacy contract.
- JSON/YAML Prettify and the accepted SQL/XML/Markdown/Text Statistics/JSON Diff/Docker production fixtures pass the unthrottled sequential Chromium `<50 ms` Long Task gates, but unmeasured main-thread transforms and a slower-device profile are not covered by that claim. The complete bounded output still resides in memory/main-thread state; only the DOM preview is truncated.
- NanoID's 100,000-symbol gate passes unthrottled Chromium at 144 ms with no observed Long Task; this is not yet a slow-device guarantee.
- JSON Schema validation passes the unthrottled Chromium 1 MiB gate, but slower-device and cross-browser performance profiles remain open. The 5-second worker deadline is the hard runtime bound.
- File Hash bounds application-owned reads to 4 MiB and its production Chromium peak-RSS gate rejects sustained whole-file growth, but `File` structured clone is browser-managed and no engine-level physical zero-copy/zeroization claim is made. Firefox/WebKit feature smoke remains part of the general cross-browser backlog because the pinned local binaries were unavailable.
- Ajv performs runtime validator code generation. The current delivery headers do not impose an eval-blocking CSP; if a future CSP does, the feature reports validator unavailability and must move to precompiled/interpreted validation rather than adding `unsafe-eval`.
- Dependency and base-image vulnerability remediation/scanner gating remains a later security track. The in-scope Mermaid, Lodash, hyparquet, and saxen chains have zero current audit paths; the current production audit still reports 57 unrelated severity-counted findings and no suppression was added.
- Reverse-proxy and real base/subpath deployment acceptance remain open.
- No upstream code has been merged, rebased, or cherry-picked. Upstream items remain requirements/fixture sources only.

## Next acceptance gates

1. Maintain the completed UI source ratchets, all-route responsive/theme/semantics matrix, and seven representative pixel baselines across all five core archetypes plus true diff.
2. Preserve the completed JSON-to-CSV, JSON Minify, and List Converter worker bounds and the source ratchet that prevents `FormatTransformer` from returning.
3. Preserve SQL's 16 KiB large-result preview plus full Copy/Blob download contract and its 700 KiB production responsiveness fixture.
4. Preserve RSA, QR/Wi-Fi QR, WYSIWYG, JSON Diff, XML/JSON/YAML/TOML/Docker converter, and bounded text-worker lifecycle/browser regressions.
5. Preserve the documented Monaco/CodeMirror and OUI product exceptions, Emoji worker gate, dev freshness contract, and measured shell dependency slice.
6. Preserve the completed JWT/HOTP boundaries and RFC vectors, the disclosed version-pinned common-license SPDX subset, and OKLCH/sRGB gamut fixtures.
7. Preserve the completed Integer Base, shared bounded-download, canonical Home URL-state, and native favorite-reorder contracts. Mandatory-precache headroom is 101,558 B raw; measure any new descriptor and lazy closure before accepting it.
8. Preserve Argon2id's strict PHC/resource/protocol limits, disposable-worker termination, independent compatibility vector, privacy/cancel source-dev flow, exact dependency pin, and separate worker/route/shell/PWA budgets.
9. Preserve Markdown Diff's byte/line/token/cell/output/time bounds, disposable-worker termination, inert sanitized preview, ephemeral-content boundary, exact DOMPurify pin, and separate route/worker budgets.
10. Preserve DNS-over-HTTPS's fixed resolver allow-list, explicit-only binary POST, strict wire/response/lifecycle bounds, URL/storage/log exclusions, and resolver-observability disclosure.
11. Preserve Mermaid's five-family prevalidation, fixed strict configuration, exact dependency pins, post-render sanitizer and sandbox, SVG/PNG allocation bounds, target-audit gate, demand loading, and explicit synchronous-DOM residual.
12. Preserve Parquet Reader's exact pin, local Blob-backed reads, disposable worker, metadata/chunk/page/output/time/memory limits, UNCOMPRESSED/SNAPPY scope, selected-page-only export, privacy boundary, and independent budgets.
13. Preserve XLSX Reader's exact dependency pin, repository-owned ZIP/OPC checks, native raw-DEFLATE boundary, disposable-worker lifecycle, independent ZIP/XML/page/output/time limits, macro/external/formula exclusions, selected-page-only export, privacy boundary, and separate route/worker budgets.
14. Generate the tool registry and metadata from one typed source before further catalog growth, or take the already scoped Emoji result-windowing/cancellable-search optimization as the next platform slice. A new product feature needs a fresh approved backlog choice.
15. Keep unrelated dependency/base-image CVE remediation and scanner policy in the separately tracked later security slice.

## Journal

- 2026-08-16: completed the separate XLSX Reader feasibility gate and the approved narrow implementation. The new lazy Data route uses exact MIT `saxen@11.1.1`, native raw-DEFLATE, and a repository-owned ZIP central-directory/OPC boundary only in a disposable worker. It rejects ZIP64, encryption, unsafe/duplicate/overlapping entries, invalid sizes/CRC, macros, external fetches, DTD/entities/extra processing instructions, non-UTF-8 XML, and every input or output limit violation before publishing metadata or one selected 200-row/32-column page. Formulas are never executed; only cached results are disclosed, scalar lexemes stay exact, and CSV formula prefixes are protected. File-derived content remains absent from URL/storage/network/analytics/logs and clears on reload. Final evidence is zero-warning lint, dual typecheck, 1417/1417 unit tests across 245 files, a 24,163-module build, 16/16 build-stat tests, 404/404 budgets, 2/2 source-dev XLSX scenarios, and all 129 routes in 5.2 minutes. Artifact: 642 files / 18,151,938 B raw / 5,303,777 B gzip; shell 844,594/247,458 B; PWA 9 files / 898,442/297,820 B; full route 135,961/47,458 B; worker 29,240/10,717 B. The target dependency has zero audit paths; current unrelated production findings remain in the deferred security track. General spreadsheet/ZIP engines and upstream implementation code were not used. See `.ai/experiments/XLSX_READER_DESIGN.md`.
- 2026-08-16: completed Parquet Reader. The new lazy Data route uses exact dependency-free MIT `hyparquet@1.28.2` only in a disposable route-owned worker. It validates local PAR1/footer/schema/row-group/column metadata and offset ranges, reads only bounded Blob slices for the selected page, and applies independent file/footer/schema/nesting/chunk/aggregate/cumulative-read/page/cell/output/time limits. Schema, metadata, 200-row/16-column paging, and conservative JSON/CSV copy/download are delivered; integers/timestamps/binary values remain exact or explicitly encoded and CSV formula prefixes are protected. The first release decodes UNCOMPRESSED/SNAPPY only; unsupported codecs remain metadata-visible, encrypted/external layouts are rejected, and `parquet-wasm`/Arrow, DuckDB-Wasm, optional compressors, full-file export, and XLSX remain outside scope. File-derived content is absent from URL/storage/network/analytics/logs and clears on reload. Final evidence is zero-warning lint, dual typecheck, 1408/1408 unit tests across 242 files, a 24,156-module build, 16/16 build-stat tests, 400/400 budgets, 2/2 source-dev Parquet scenarios, and all 128 routes in 4.3 minutes. Artifact: 639 files / 18,100,903 B raw / 5,285,852 B gzip; shell 842,926/247,222 B; PWA 9 files / 896,774/297,584 B; full route 183,372/61,028 B; worker 65,630/20,682 B. The target dependency has zero audit paths. PR #1529 supplied requirements only; no upstream implementation code was copied.
- 2026-08-16: completed Mermaid Diagram Renderer/Exporter. The new lazy Development route accepts only Flowchart, Sequence, Class, State, and ER input, rejects frontmatter/init, HTML, URLs, click/link/style directives and unsupported families before demand-loading exact `mermaid@10.9.8`, then independently sanitizes and bounds SVG before placing it in a scriptless network-denying sandbox. SVG copy/download and bounded PNG rasterization operate only on the sanitized result; content is ephemeral and object URLs are revoked. Cancel/edit/Clear/unmount invalidate late results, while the UI honestly discloses that the synchronous DOM phase cannot be physically interrupted. Audit rejected vulnerable `10.9.6`; patched `10.9.8` plus shared `lodash`/`lodash-es@4.18.1` now have zero target advisories and reduce the repository audit from 66 to 63 unrelated findings. Final evidence is zero-warning lint, dual typecheck, 1399/1399 unit tests across 240 files, a 24,149-module build, 16/16 build-stat tests, 396/396 budgets, 2/2 source-dev Mermaid scenarios, and all 127 routes in 4.4 minutes. Artifact: 636 files / 18,011,920 B raw / 5,257,890 B gzip; shell 841,236/246,993 B; PWA 9 files / 895,084/297,355 B; route 44,906/18,043 B; renderer 278,266/78,124 B. Orca 1.4.177 again exited immediately after reporting ready (`runtime_unavailable`, macOS task-name failure), so deterministic Playwright source-dev acceptance supplied the live UI evidence. No upstream implementation code was copied.
- 2026-08-16: completed the measured DNS-over-HTTPS Query goal. The new lazy Network route sends one explicit RFC 8484 binary POST to a fixed browser-compatible Cloudflare standard or malware-blocking endpoint; no query is issued during typing. A dependency-free wire encoder/parser supports ten common record types and strictly bounds names, compression pointers, question identity, flags, EDNS extended status, message/record/output size, content type, redirects, streaming chunks, timeout, replacement, cancellation, clear, and unmount. The UI states that the resolver sees the DNS name/type, client IP, and ordinary transport metadata and that standard DoH is not local-only, anonymous, or ODoH. Query/result content is absent from URL/storage/analytics/application logs. Final evidence is zero-warning lint, dual typecheck, 1373/1373 unit tests across 238 files, a 23,226-module build, 346/346 budgets, 1/1 controlled source-dev privacy/cancel flow, a live Orca Cloudflare request, and all 126 routes. The artifact is 589 files / 14,643,752 B raw / 4,229,913 B gzip; shell is 839,213/246,351 B, PWA is 9 files / 893,061/296,713 B, and the route adds 26,124/11,383 B raw/gzip. PR #1371 supplied requirements only; no upstream implementation code was copied.
- 2026-08-16: completed the measured Markdown Diff goal. The new lazy Text route performs deterministic line or exact whitespace/punctuation-preserving word/token source comparison in a disposable route-owned worker. Each side is limited to 256 KiB, 4,000 lines or 8,000 tokens; individual lines, LCS cells, output, protocol, and runtime are independently bounded. Success, error, cancel, replacement, input change, timeout, clear, and unmount physically terminate owned work. Optional previews render only the last successful 64-KiB-per-side snapshot through existing lazy `markdown-it` with raw HTML, active links, and images disabled, then through a static DOMPurify allow-list; DOMPurify is upgraded and exactly pinned at patched `3.4.13`. Exact snapshot comparison avoids delimiter collisions and repeated large-string concatenation. Input/report/preview data is absent from URL/storage/network transport/errors, and no editor/diff dependency or upstream code was added. Final evidence is zero-warning lint, dual typecheck, 1363/1363 unit tests across 236 files, a 23,221-module build, 344/344 budgets, 1/1 source-dev comparison/sanitization/privacy/cancel, and all 125 routes in 4.1 minutes. The artifact is 587 files / 14,624,060 B raw / 4,222,289 B gzip; shell is 838,311/246,134 B, PWA is 9 files / 892,159/296,496 B, the worker is 4,927/2,021 B, and the route adds 168,815/71,948 B raw/gzip. Issue #1804 and PR #1803 supplied requirements only.
- 2026-08-16: completed the measured Argon2id Hash & Verify goal. The new lazy Crypto route pins `hash-wasm@4.12.0` and loads its Argon2 v1.3 WASM/glue only inside one disposable route-owned worker. Hashing uses a Web Crypto 16-byte salt and the RFC 9106 memory-constrained default (`m=65536`, `t=3`, `p=4`, 32-byte tag); verification accepts only strict canonical Argon2id `v=19` PHC strings within password/salt/hash/memory/iteration/lane/time bounds. Success, error, cancel, replace, timeout, input change, clear, and unmount physically terminate owned work; password/PHC/result data is absent from URL/storage/network transport/error text and the UI explicitly avoids password-database and guaranteed-zeroization claims. Independent `@phc/argon2` compatibility and RFC parser vectors, malformed/oversized/protocol/lifecycle tests, 1/1 source-dev hash/verify/privacy/cancel, and the 124-route smoke pass. Final evidence is zero-warning lint, dual typecheck, 1343/1343 unit tests across 231 files, a 23,134-module build, and 340/340 budgets. The artifact is 583 files / 14,496,638 B raw / 4,167,585 B gzip; shell is 837,262/245,970 B, PWA is 9 files / 891,110/296,332 B, the worker is 34,523/13,687 B, and the route adds 130,867/47,124 B raw/gzip. No upstream implementation code was copied.
- 2026-08-16: completed the shell-conscious existing-route interoperability wave. Integer Base now parses exact signed bases 2–64 with documented common notation, correct case semantics, strict malformed/ambiguous fixtures, and a 4,096-character bound. Seventeen structured-text/authoring paths share one download helper/contract over already-computed bounded output. Home filtering uses canonical bounded URL state with deep-link/history coverage and native pointer plus keyboard favorite reorder; the final source-dev audit caught and closed focus loss when an activated edge action became disabled. Removing `vuedraggable` and `sortablejs` cuts shell and mandatory Workbox by 107,897 B raw / 38,855 B gzip and restores 110,057 B raw PWA headroom under unchanged ceilings. Lint, dual typecheck, 1325/1325 unit tests across 227 files, a 23,128-module build, 336/336 budgets, 5/5 targeted source-dev scenarios, and the fresh all-123-route smoke in 4.1 minutes pass.
- 2026-08-16: completed the DevOps/JSON/crypto-authoring wave. DevOps Configuration extracts deterministic shell-safe `.env` assignments from a selected JSON/YAML/TOML subtree with exact pointer/prefix validation and no execution/expansion. JSON Schema & Code Generator produces bounded deterministic RFC 6902 patches with RFC 6901 escaping, array semantics, and source-lexeme-preserving numeric values. HMAC accepts explicit UTF-8/hex/canonical Base64 keys in a disposable worker and passes RFC 4231; legacy Encryption now discloses its password-envelope/salt/derived-IV semantics instead of exposing ambiguous raw-key controls. Token Generator adds numeric bounded batches, custom/denied alphabets, unbiased Web Crypto sampling, copy/download/clear, and harmless preferences only. No runtime dependency or upstream code was added. Final evidence is zero-warning lint, dual typecheck, 1291/1291 unit tests across 225 files, a 23,132-module build, 336/336 artifact checks, all 123 routes in 3.8 minutes, and 8/8 targeted/regression scenarios. The build is 582 files / 14,557,612 B raw / 4,188,317 B gzip; shell is 943,992 B / 284,657 B gzip and PWA is 9 files / 997,840 B / 335,019 B gzip.
- 2026-08-16: completed the security/search/JSON interoperability wave. Added a lazy disposable-worker Ansible Vault & htpasswd Helper with byte-compatible Vault 1.1/labelled 1.2 AES256, authenticated parsing, Apache bcrypt `$2y$` generation/verification, explicit version/cost controls, strict limits, generic failures, and ephemeral secrets; compatibility fixtures come from Ansible Core 2.21.2 and Apache documentation. Command Palette now ranks exact title/keyword/normalized-path matches before deterministic fuzzy results and provides accessible Show all without stale keyboard selection. JSON Repair & Query adds explicit strict outer-string unescape and preserves the decoded JSON text/numeric lexemes. No runtime dependency or upstream code was added. Final evidence is zero-warning lint, dual typecheck, 1278/1278 unit tests across 222 files, a 23,117-module build, 334/334 artifact checks, all 123 routes, and 4/4 targeted scenarios. The build is 581 files / 14,423,193 B raw / 4,149,937 B gzip; shell is 943,748 B / 284,554 B gzip and PWA is 9 files / 997,596 B / 334,916 B gzip.
- 2026-08-16: completed the tabular/Wi-Fi/permissions interoperability wave. Added a lazy disposable-worker CSV/TSV inspector with exact quoting/newline/empty-cell handling, bounded profile/preview, explicit string or conservative JSON inference, normalized export, formula-prefix protection, local file/paste input, and no XLSX dependency. Wi-Fi QR exposes exact generated payloads and maps WPA3/transition intent to the broadly scanner-compatible de-facto `T:WPA` form with an explicit no-SAE-enforcement warning. Chmod now round-trips octal/symbolic modes, special bits, and creation umasks without executing commands or claiming ACL equivalence. No runtime dependency or upstream code was added. Final evidence is zero-warning lint, dual typecheck, 1268/1268 unit tests across 219 files, a 23,110-module build, 330/330 artifact checks, all 122 routes, and 3/3 targeted scenarios. The build is 579 files / 14,382,350 B raw / 4,132,736 B gzip; shell is 941,305 B / 283,871 B gzip and PWA is 9 files / 995,153 B / 334,233 B gzip.
- 2026-08-16: completed the JWT/HOTP, SPDX, and OKLCH interoperability wave. JWT now separates decode-only, unsigned authoring, HS256/384/512 signing, and algorithm-driven verification under compact-token/JSON/depth/key limits, with untrusted-content and authentication-scope warnings; HOTP adds exact unsigned-64-bit counters, strict bounded Base32, 6/8 digits, provisioning URIs, and RFC 4226 Appendix D vectors. A new lazy SPDX route provides bounded search and conservative family-level triage across an explicitly incomplete 17-license subset pinned to SPDX License List 3.28.0 (2026-02-20), with canonical links and a not-legal-advice warning. Color Converter adds CSS Color 4 OKLab/OKLCH matrices, strict parsing, sRGB gamut detection, and constant-L/H chroma mapping without claiming Local MINDE/display-profile management. No runtime dependency or upstream code was added. Final evidence is zero-warning lint, dual typecheck, 1257/1257 unit tests across 216 files, a 23,105-module build, 326/326 artifact checks, all 121 routes, and 4/4 targeted scenarios. The build is 577 files / 14,359,816 B raw / 4,123,947 B gzip; shell is 939,833 B / 283,608 B gzip and PWA is 9 files / 993,681 B / 333,970 B gzip.
- 2026-08-16: completed the bounded storage/privacy/crypto wave. Added RAID 0/1/5/6/10 capacity planning with exact BigInt fractions, SI/IEC units and conditional RAID 10 resilience semantics; lossless signature-verified JPEG/PNG/WebP container metadata removal without decode/recompression; and a versioned ITAE-v1 Web Crypto AES-256-GCM text/file envelope with PBKDF2-HMAC-SHA-256 at 600,000 iterations, fresh salt/IV, authenticated header, encrypted file metadata, bounded disposable-worker lifecycle, and generic authentication failure. No runtime dependency or upstream implementation code was added. Final evidence is zero-warning lint, dual typecheck, 1243/1243 unit tests across 213 files, a 23,104-module build, 324/324 artifact checks, all 120 routes in 3.8 minutes, and 3/3 targeted scenarios. The build is 578 files / 14,338,885 B raw / 4,116,483 B gzip; shell is 938,834 B / 283,367 B gzip and PWA is 9 files / 992,682 B / 333,729 B gzip. Worker-transitive `js-base64` is explicitly prebundled and the clean traversal has no optimizer restart.
- 2026-08-16: completed the Priority B local utility wave. Added worker-owned set/multiset/ordered List Comparison with a hard LCS cell ceiling; Unicode/UTF/grapheme/GSM-7 inspection with non-BMP fixtures and disclosed SMS-estimate limits; strict Gregorian ↔ ISO week conversion plus RFC 5545 UTC VEVENT escaping/folding/inspection; and non-executing HTTP cURL/fetch authoring with duplicate fields, bounds, default recognized-secret redaction, and strict POSIX cURL import through the existing CLI tokenizer. No runtime dependency or upstream implementation code was added. Final evidence is zero-warning lint, dual typecheck, 1223/1223 unit tests across 206 files, a 23,089-module build, 314/314 artifact checks, all 117 routes, and 5/5 targeted scenarios. The build is 573 files / 14,281,568 B raw / 4,096,501 B gzip; shell is 935,687 B / 282,899 B gzip and PWA is 9 files / 989,535 B / 333,261 B gzip. The same fresh-dev traversal exposed and then verified the `jsonc-parser`/Monaco lazy-route prebundle contract: all routes now load without a Vite optimizer restart.
- 2026-08-16: completed the Priority B browser-native wave. Added secure unbiased passphrases from a route-local wordlist; bounded native GZIP text/file streams; RFC 3492 IDN conversion with canonical validation and heuristic safety warnings; exact BigInt-rational data/transfer units; quoted CSV/TSV-to-Markdown in a disposable bounded worker; binary32/binary64 IEEE-754 fields, exact stored values and rounding errors; and a non-executing Conventional Commit helper. All seven routes are lazy, vertical, local-only, and ephemeral; unsupported native capabilities and non-goals are explicit. No runtime dependency or upstream implementation code was added. Final evidence is zero-warning lint, dual typecheck, 1205/1205 unit tests across 200 files, a 23,072-module build, 304/304 artifact checks, all 113 routes, and 7/7 targeted scenarios. The build is 567 files / 14,237,148 B raw / 4,079,885 B gzip; shell is 932,254 B / 282,277 B gzip and PWA is 9 files / 986,102 B / 332,639 B gzip.
- 2026-08-16: completed the bounded offline utility wave. Developer Text Workspace provides worker-owned stack-trace normalization, bounded literal/regex replacement, safe folder trees, and Markdown TOC without executing/rendering input; Network Calculation Suite provides exact IPv4/IPv6 CIDR range/membership/exclusion plus bounded DHCP Option 43, port, and TTL helpers; SAML & Enterprise Timestamp Inspector performs local Base64/raw-DEFLATE decode, plain-text XML formatting, DTD/entity rejection, explicit non-verification disclosure, exact FILETIME and strict LDAP/ISO conversions; Favicon & App Icon Generator creates seven bounded PNG variants, a manifest fragment, and a deterministic TAR from one ephemeral local image with deterministic bitmap/URL disposal. Paste-as-Markdown, duplicate MAC conversion, and a mislabeled ZIP were deliberately not claimed. No runtime dependency or upstream code was added. Final evidence is zero-warning lint, dual typecheck, 1182/1182 unit tests across 192 files, a 23,043-module build, 288/288 artifact checks, all 106 routes, and 6/6 targeted scenarios. The build is 557 files / 14,178,387 B raw / 4,056,253 B gzip; shell is 926,179 B / 281,221 B gzip and PWA is 9 files / 980,027 B / 331,583 B gzip.
- 2026-08-16: completed the Priority A closure wave. URL Encoder now adds explicit tracker removal without echoing values, defang/refang, UTM, and text-fragment authoring; Local File Inspector adds magic-byte detection, streaming CRC-32, and a bounded hex preview; JSON Schema & Code Generator produces Draft 2020-12 schema, TypeScript, or graph statistics through one bounded JSON5 parse; Ed25519/SSH generation runs explicitly in a disposable Web Crypto worker with SPKI/PKCS#8/OpenSSH/fingerprint output and clear capability disclosure; QR Decoder/OTP Import owns bounded image/current-frame decoding, explicit five-minute camera access, and cross-browser TOTP/HOTP URI parsing. Files, JSON examples, OTP secrets, and private keys remain ephemeral; no runtime dependency or upstream code was added. Final evidence is zero-warning lint, dual typecheck, 1160/1160 unit tests across 186 files, a 23,019-module build, 276/276 artifact checks, all 102 routes, and 10/10 targeted scenarios on an isolated fresh preview port. The build is 550 files / 14,120,692 B raw / 4,033,727 B gzip; shell is 921,663 B / 280,428 B gzip and PWA is 9 files / 975,511 B / 330,790 B gzip.
- 2026-08-16: completed the second Priority A feature wave. Added CLI Command Editor with exact unchanged round trips and safe edited quoting; UUID v6/v7 plus UUID/Mongo ObjectID/Snowflake inspection; bounded worker-owned Dockerfile/Compose/nginx/properties workflows; bounded JSON5 repair and non-executable JSONPath-like query; and bounded structural certificate/CSR/public-key inspection with Web Crypto SHA-256 fingerprints and private-key rejection. No runtime dependency or upstream implementation code was added. A full-route smoke exposed and closed an older automatic Camera permission rejection by moving permission requests behind the visible user action; an adversarial Snowflake boundary review also closed safe-integer timestamps outside the ECMAScript Date range. Final evidence is zero-warning lint, dual typecheck, 1123/1123 unit tests across 178 files, a 22,998-module build, 262/262 artifact checks, all 98 routes, and all 10 targeted feature scenarios. The build is 541 files / 14,035,194 B raw / 4,004,069 B gzip; shell is 917,063 B / 279,612 B gzip and PWA remains 9 files / 970,911 B / 329,974 B gzip.
- 2026-08-16: completed the requested seven-feature Priority A product goal. Added five lazy routes—Faker-compatible Mock Data Generator, BigInt IPv6 Calculator/CIDR Toolbox, Sensitive Data Masker/HAR Sanitizer, Timezone + Date Duration Calculator, and Barcode Generator/Reader—and expanded Docker conversion in both directions plus Cron Next Runs with Unix/seconds/Quartz and IANA timezone presets. The generation/sanitization/Cron/Docker paths are bounded and worker-owned where structured work can amplify; uploaded/sensitive/generated content remains ephemeral; barcode reading is local and capability-detected; no runtime dependency was added. Final evidence is zero-warning lint, dual typecheck, 1094/1094 unit tests across 171 files, a 22,905-module production build, 248/248 artifact checks, all 94 registry routes in Chromium, and all 17 targeted feature scenarios. The build is 534 files / 13,833,668 B raw / 3,936,666 B gzip; shell is 912,165 B / 278,540 B gzip, only +4,858 B raw / +823 B gzip over the preceding 89-route checkpoint because the new implementation remains lazy.
- 2026-08-16: completed the performance-first goal before new catalog development. JSON-to-CSV now parses JSON5 once in a bounded worker with incremental CSV/output-amplification limits; JSON Minify reuses the structured JSON worker; measured List Converter runs in a dedicated bounded worker; and `FormatTransformer` is removed. SQL and the three new large transformer results use a 16 KiB preview while retaining the complete bounded result for Copy/Blob download. The isolated worker-backed CodeMirror spike measured 363,669 B raw / 118,937 B gzip and passed its 1 MiB worker/Long Task gate, but was rejected because async public `MergeView` replacement loses focus, undo, and history. Emoji search now owns a 400,601 B / 68,523 B gzip bounded worker and passes its 4x CPU route gate. OUI exact coverage is an accepted privacy-driven exception. Seven shell-critical Lodash imports were replaced for an apples-to-apples -433 B gzip result; Vite dev explicitly optimizes worker-only dependencies; frozen install pins the compatible Unhead/VueUse graph. Final lint/typecheck, 1021/1021 unit, 16/16 build-stat, 232/232 artifact, 4/4 OUI, source-dev 12/12, focused production performance 11/11, NanoID 4/4, File Hash/reactive coverage, build, and diff checks pass. All 176 Chromium cases pass across the isolated performance/full functional matrix; a single combined timing run is intentionally not claimed under measured unrelated workstation CPU saturation. Faker-compatible bounded mock data generation is recorded as Priority A / Text for the next product phase. Orca again exited after reporting `ready`, so the documented deterministic browser fallback was used.
- 2026-08-16: eliminated the recurring dev/preview cache ambiguity. The old runbook reused port 8080 for source dev and production preview while Orca itself occupied Vite's default 5173; two IT Tools dev processes were also simultaneously live on 8080 and 8091. The canonical contracts are now strict `127.0.0.1:8091` dev and strict `127.0.0.1:5050` preview. Dev emits `Cache-Control: no-store` and `X-IT-Tools-Mode: development`, never registers Workbox, removes only the exact root IT Tools registration plus three application cache prefixes, and reloads a controlled document at most once. `pnpm dev:fresh` forces Vite optimizer rebuilds. The duplicate 8080 process was stopped; 8091 remained source-served with `/@vite/client` and `/src/main.ts`. Final lint/typecheck, 990/990 unit across 150 files, a 22,876-module production build, 16/16 build-stat tests, 228/228 artifact budgets, 2/2 production Workbox flows, and the deterministic dev-runtime Chromium smoke pass. The latter confirms no controller/registrations, deletes the seeded IT Tools cache, and preserves an unrelated cache. Dev-only code tree-shakes out, leaving shell and mandatory-PWA bytes unchanged. Orca 1.4.177 exited after `ready` twice, so its runbook-required deterministic fallback was used and manual embedded-browser acceptance remains unavailable for this slice.
- 2026-08-15: completed the measured Hash Text hardening slice. A static benchmark ranked eight reactive CryptoJS digests over 1 MiB at about 706 ms, ahead of JSON-to-CSV's duplicated JSON5 parse at 451 ms, AES at 69 ms, 150,000-line list work at 55 ms, and HMAC SHA-256 at 22 ms. Hash Text now uses a strict route-owned worker with 16 KiB live / 1 MiB hard input / 8 KiB result / five-second bounds, exact digest keys, static no-echo errors, physical cancellation, prior-result retention, and empty/Unicode/privacy regressions. Its 1,048,576-byte production fixture measured 578 ms cold-route, 2,951 ms result-ready, 0.0 ms longest observed Long Task, and 298 heartbeat ticks. Focused unit tests pass 8/8, Vite-dev Chromium passes 3/3, full unit passes 987/987 across 149 files, build-stat/budget gates pass 16/16 and 228/228, and the clean sequential production-preview gate passes 172/172 in 4.1 minutes without retries or threshold changes. The worker is 60,239 B raw / 21,154 B gzip; its full additional closure is 77,014 B / 29,635 B versus 62,200 B / 22,934 B before isolation. JSON-to-CSV is the next measured main-thread target.
- 2026-08-15: completed the XML/Docker bounded-parser goal. XML-to-JSON and JSON-to-XML now reuse the vertical `BoundedTextTransformer` with one parse-once XML-family worker at 64 KiB live / 1 MiB input / 2 MiB output / eight seconds. Docker Run-to-Compose moves composerize, obsolete-version cleanup, and message classification into a strict worker at 16 KiB live / 256 KiB input / 512 KiB YAML / four seconds, with bounded messages and Blob downloads; accepted Compose/message semantics are unchanged. Production fixtures measured XML-to-JSON 819,213 B at 379/179 ms and Docker 20,796 B at 363/167 ms cold-route/result-ready, both with zero observed Long Tasks. Final integrated evidence is lint/typecheck, 980/980 unit across 146 files, a 22,872-module build, 226/226 budgets with thirteen independent worker ceilings, and 168/168 sequential Chromium E2E.
- 2026-08-15: completed the next bounded-converter/performance goal. JSON-to-TOML/YAML, YAML-to-JSON/TOML, and TOML-to-JSON/YAML now share a vertical `BoundedTextTransformer`, one strict transport, and three parse-once source-family workers with 64 KiB live, 1 MiB input, 2 MiB output, and eight-second limits. Eleven focused unit regressions plus all original and large/error browser flows pass; YAML scalar `false`/`0` output and the prior YAML-to-TOML error-label defect are covered. Production Chromium evidence records cold-route/result-ready timing, heartbeat, and Long Tasks for SQL, XML, Markdown, Text Statistics, and JSON Diff. The stable fixtures report no task `>=50 ms`; repeated probes separately record SQL's ordinary textarea publication crossing 50–56 ms at 400–700 KiB, so that rendering ceiling remains open without weakening the gate or raising limits. Final integrated evidence is lint/typecheck, 971/971 unit, a 22,973-module build, 222/222 budgets, and 162/162 sequential Chromium E2E.
- 2026-08-15: completed the requested three-part follow-up. Expanded deterministic state coverage from three to seven Darwin Chromium baselines across every core UI archetype plus true diff, retaining portable semantic assertions. JSON Diff is now an explicit route-owned worker with 1 MiB-per-side, depth-128, 100,000 input/output-node, 250,000-LCS-cell, and eight-second bounds; it aligns unique `id`/`key`/`name` object arrays and primitive LCS sequences with positional fallback. Math, SQL, XML, Markdown, and Text Statistics use route-local terminate-and-replace workers with shared debounce/explicit action/status/disposal behavior; Math moved to the number-only entry and its route closure fell from 623,620 B / 180,069 B gzip to 363,883 B / 108,586 B. Final lint/typecheck, 960/960 unit, 23,049-module production build, 216/216 budgets, 21/21 focused Vite-dev Chromium flows, and 154/154 sequential production-preview Chromium flows pass. The full gate also replaced three stale pre-unification E2E selectors with stable accessible/test-id contracts and retained strict page-memory guards while accounting for variable Chromium process startup RSS.
- 2026-08-15: completed the first representative visual-state and reactive-computation hardening slice. Added three deterministic Darwin Chromium baselines with cross-platform semantic assertions for task loading/disabled controls, mobile-dark validation errors, and dense-form long-value/results. QR/Wi-Fi QR now debounce/order/limit generation and clear incomplete output; WYSIWYG uses a bounded terminate-and-replace Prettier worker with idle/manual thresholds; JSON Diff replaces the reproduced 6,800-read deep-equality path with one traversal and progressive 200-row rendering. Full lint/typecheck, 939/939 unit, production build, 204/204 budgets, and 14/14 focused Vite-dev Chromium flows pass. Orca 1.4.177 was invoked through the documented CLI, but its runtime exited between `status` and `goto` twice with `runtime_unavailable`; no alternate binary or stale preview was used, and deterministic Chromium E2E supplied the browser gate.
- 2026-08-15: completed the RSA Key Pair Generator correctness and UI-contract task. Replaced the reactive arbitrary `+/- 8` node-forge flow with fixed 2,048/3,072/4,096-bit presets and an explicit Web Crypto task in a strict route-owned terminate-and-replace worker. Cancel, replacement, timeout, and unmount now physically terminate owned work; previous keys survive pending/cancel/error and remain session-only. Direct `node-forge`/types were removed while the transitive PDF path remains. Source ratchets now report zero direct route form-items/number-inputs/literal label widths. Focused RSA tests pass 22/22, full unit 921/921, Chromium Vite-dev 2/2, lint/typecheck/build, and 202/202 budgets pass. The additional route closure fell from 450,149 B raw / 132,813 B gzip to 60,832 B / 24,700 B; its worker is 2,498 B / 1,151 B.

- 2026-08-10: completed the catalog-wide UI consistency rollout. Every tool route now opts into a named layout archetype; non-diff transformations are always vertical, direct route-level Naive text/select/switch/checkbox/color/input-group controls are zero, and RSA is the single source-guarded form/number/label-width exception pending its functional preset-and-Generate repair. The all-89-route Vite-dev matrix passes desktop/light and mobile/dark overflow, visible form-control naming, theme, content, and runtime/chunk checks. Final gates pass with zero-warning lint, dual-project typecheck, 901/901 unit tests across 120 files, a fresh 24,205-module production build, 202/202 artifact budgets, and `git diff --check`. Screenshot and extended state fixtures remain hardening backlog.
- 2026-08-10: completed the broad numeric/form/catalog UI wave. Added a keyboard-operable `c-color-picker`; migrated Bcrypt, ASCII Art, ETA, Integer Base, Roman Numeral, Temperature, BIP39, Benchmark Builder, Chmod, Hash Text, Keycode Info, Open Graph, Date-time, IPv4, OTP, URL Parser, Case Converter, HTML Entities, Markdown, SafeLink, and the QR/color family. Fixed Bcrypt's stale-rounds action bug caused by Naive UI 2.35 deferring `precision=0` updates until blur. Tool source now has zero direct Naive checkbox/switch/color/fixed-input-group controls and one guarded form-item/number/label-width exception, all in queued RSA. Focused evidence includes 29/29 Bcrypt unit/component and 4/4 Bcrypt E2E, 57/57 Date/IPv4/OTP/URL/Camera unit, existing Date/OTP E2E 6/6 after semantic locator repair, and the expanding responsive control-rollout suite against Vite dev. Orca confirms QR color triggers are labelled keyboard buttons, the page loads `/@vite/client`, no service worker controls it, and no desktop overflow is present.
- 2026-08-10: completed the route-wide boolean-control rollout: direct `n-switch` usage fell from 24 audited instances to zero, with a global source ratchet. Crontab now uses a wide labelled expression plus three responsive mode switches; String Obfuscator uses equal numeric/switch columns; Text Diff uses a labelled privacy switch with its description; SVG Placeholder uses the generator rhythm, full-width numeric fields, bounded preview, outputs, and actions. Focused control tests pass 14/14 and SVG/Crontab/Text Diff dev E2E pass 4/4.
- 2026-08-10: expanded the UI rollout with `c-choice-group`/`c-checkbox`, `c-switch`, and `c-slider`; upgraded `c-input-number` to publish spinbutton min/max/current state. Migrated Regex, Token, Random Port, MAC Address, IPv6 ULA, Lorem Ipsum, List/Base64 switches, JSON Diff, and JSON/YAML/XML formatter options. Focused gates pass: 15/15 generator/control tests, 20/20 switch/form tests, 38/38 formatter component/contracts, 9/9 Lorem/List/JSON Diff E2E, and 5/5 JSON/YAML/XML E2E against Vite dev. Orca confirms equal-width desktop blocks, one-column 390 px MAC layout with no overflow, dark theme, `/@vite/client`, `/src/main.ts`, and no service-worker controller. The integrated checkpoint passes zero-warning lint, dual-project typecheck, 887/887 unit tests across 118 files, a fresh 24,207-module production build, 202/202 build-budget checks, and `git diff --check`; full all-route E2E remains for the final rollout gate.
- 2026-08-10: added the shared `c-field` and `c-input-number` contracts, migrated List Converter, Wi-Fi QR, Percentage Calculator, UUID, and ULID, and added source/component regressions. Orca Vite-dev review confirms List input/output parity, the Wi-Fi result below its form after valid WPA input, equal Percentage groups with nine native label targets, and `25% of 200 = 50`; no reviewed page had a service-worker controller. Zero-warning lint, typecheck, 870/870 unit, production build, 202/202 budgets, and 8/8 focused Chromium E2E against the live Vite server pass. RSA's failing reactive `+/- 8` key-size generation is separately queued as an explicit preset-and-Generate correctness task.
- 2026-08-10: resumed UI consistency as an explicit long-running goal. Delivered shared tokens and accessible P0 controls; adopted input-above-output at equal available width for all identified non-diff editor transformers; migrated JSON/YAML/SQL plus the text conversion family; and unified UUID/ULID/NanoID on NanoID's options -> fixed output -> actions contract. Source contract tests and live Orca dev geometry guard the decisions. The remaining task/dense/numeric/catalog rollout and full responsive light/dark acceptance remain active.
- 2026-08-10: completed the source-level UI consistency inventory across shared primitives and all tool SFCs. Confirmed mixed custom/Naive field systems, ad hoc boolean and action semantics, widespread literal widths, missing visual regression gates, and P0 accessibility/state defects in shared controls. Recorded `.ai/UI_CONSISTENCY.md` and promoted its live baseline, primitive repair, and representative pilot ahead of the fourth catalog feature; live browser acceptance remains open because the in-app browser was unavailable during this audit.
- 2026-07-18: started implementation from the approved roadmap and established the baseline.
- 2026-07-18: completed Token/TOTP secure randomness, alphabet/boundary tests, and privacy coverage.
- 2026-07-18: implemented Text Diff minimal Monaco import, worker ownership, disposal, opt-in bounded persistence, and schema-v3 route measurements.
- 2026-07-18: reduced Text Diff JS gzip by 28.8%; payload remains over budget.
- 2026-07-18: passed the 86-route smoke and removed ASCII Art's failing `unpkg.com` dependency using local on-demand fonts.
- 2026-07-18: added forced-GC gates; Text Diff retains +2.46 MiB with zero workers after ten cycles.
- 2026-07-18: found that the apparent remaining Monaco leak was a global dynamic-layout leak; persistent `BaseLayout` reduced Home/tool retained growth from +188.40 MiB to +0.86 MiB.
- 2026-07-18: passed rootless/read-only default and arbitrary-UID container contracts with strict static/cache/privacy behavior.
- 2026-07-18: hardened release ordering/permissions/versioning and enabled SBOM/provenance declarations; vulnerability remediation/gating was moved to the later security track by scope direction.
- 2026-07-18: started the next project-owned correctness slice for Base64 File, Temperature Converter, and URL Parser; external/base-image CVE work remains deferred.
- 2026-07-18: completed correctness slice 1: seven project-owned fixes, 65/65 focused regressions, scoped lint, and diff checks are green; started Regex/Bcrypt/Camera lifecycle work in parallel.
- 2026-07-18: completed Regex optional-capture and shared `TextareaCopyable` fixes; activated 181 build-budget checks in CI/release with explicit rationales for existing heavy routes.
- 2026-07-18: completed Bcrypt worker/cancellation, Camera Recorder lifecycle, URL Encoder standards modes, and UnoCSS native-size fixes with focused unit/component/browser gates.
- 2026-07-18: completed the current persistence inventory: thirteen legacy content/network-input keys are cleaned and session-only, secret classes remain ephemeral, and policy/remaining UX work are recorded in `.ai/PERSISTENCE.md`.
- 2026-07-18: passed the Text Diff two-by-1-MiB worker interaction gate (725 ms ready, 45 ms UI action) and completed the eight-caller single-execution refreshable-task migration.
- 2026-07-18: reduced mandatory PWA install from 270 files / 6.12 MB raw to nine files / 945,188 B raw / 324,178 B gzip and made shell/runtime/static membership plus both product byte targets executable gates.
- 2026-07-18: exposed the persistence boundary in-product and added a confirmed global reset over the audited managed-key registry without calling broad `localStorage.clear()`.
- 2026-07-18: moved Regex matching and RandExp into bounded terminate-and-replace workers; 35/35 unit/component plus 4/4 Chromium checks cover zero-width results, sample projection, catastrophic timeout/heartbeat, stale work, route disposal, and previous-tool unmount. DOM-dependent SVG is explicit and bounded rather than falsely reported as worker-backed.
- 2026-07-18: added the shared 100,000-byte plain-output threshold and passed a 1 MiB JSON fixture with one `<pre>`, zero result descendants, and reachable copy; closed mobile route/Escape behavior with 3/3 checks.
- 2026-07-18: paged Emoji 60 -> 120, reduced initial DOM to 1,731 elements and additional-route gzip by 48.8%, lazy-loaded keywords, preserved Fuse ranking in one result grid, and passed 10/10 focused plus 3/3 Chromium checks.
- 2026-07-18: the first real offline test exposed a missing pre-control Workbox client chunk; added fail-closed manifest discovery, then passed clean-HTTP-cache offline reload with all shell/lazy responses served by the service worker.
- 2026-07-18: final integrated checkpoint passed lint, dual typecheck, 367/367 unit across 70 files, 11/11 build-stat tests, 194/194 artifact budgets, production build, 86/86 Chromium tests, and all 86 registered routes; refreshed the schema-v3 baseline.
- 2026-07-18: post-checkpoint review closed RandExp pre-allocation amplification (nested repetition and skipped/lookahead capture numbering), restored zero-width Regex results, made SVG single-flight, fixed Emoji symbol sizing, and made mobile-menu hiding inert with deterministic focus restoration; all combined gates pass.
- 2026-07-18: bounded Camera recordings to 5 minutes/64 MiB with 1-second chunks, screenshots before canvas allocation and after encoding with single-flight/late-callback guards, and retained captures to 128 MiB plus count caps; Camera 18/18 and shared-button 4/4 focused regressions plus the full Chromium suite pass.
- 2026-07-18: final independent audit closed Regex URL write-back and analytics disclosure, late/simultaneous Camera screenshots, transient mobile-state persistence, desktop inertness, and Emoji relevance-order regressions; all focused checks pass.
- 2026-07-18: final gates pass on the integrated tree: zero-warning lint, dual typecheck, 367/367 unit, production build, 11/11 build-stat tests, 194/194 artifact checks, 86/86 Chromium E2E in 22.7 seconds, and `git diff --check`; external CVE work remains intentionally deferred.
- 2026-07-18: started the next autonomous P0/P1 slice with OUI partitioning, YAML worker isolation, offline uncached-route recovery, and a measured Text Diff editor decision; the external CVE/dependency track remains deferred.
- 2026-07-18: generated and measured 112 deterministic OUI buckets at about 66.9 kB gzip for the worst request, then rejected that prototype because its prefix-derived filenames would disclose MAC input in nginx `$uri`; no bucket implementation remains in the accepted tree.
- 2026-07-18: completed YAML Prettify's parse-once bounded worker, explicit large-input flow, 19/19 focused checks, and 1 MiB Chromium heartbeat fixture; the owned worker is 96,806 B raw / 30,216 B gzip.
- 2026-07-18: completed the Text Diff Monaco/CodeMirror experiment and schema-v4 owned-worker accounting. Repaired Monaco remains because CodeMirror needs a worker-backed correctness spike; the true current closure is 2,474,507 B raw / 646,984 B gzip.
- 2026-07-18: completed OUI fixed-worker privacy/lifecycle acceptance from one reproducible 1,986,393-byte compact artifact. Generator 4/4, focused 28/28, Chromium 2/2, and the reviewed 1,937,380 B raw / 770,460 B gzip closure gate pass; the `<250 kB` product target remains open.
- 2026-07-18: completed production-Workbox uncached-route recovery. The integration test exposed sticky failed native imports, so retry now verifies the app origin and starts a query-free fresh document, preserving recovery while keeping any in-memory query/hash out of access logs.
- 2026-07-18: completed the shared/direct randomness audit in `.ai/RANDOMNESS.md` and replaced UUID v1 pseudo-random node/clock seeding with Web Crypto plus 8/8 regression checks.
- 2026-07-18: final slice gates pass: zero-warning lint, dual typecheck, 436/436 unit across 81 files, OUI generator 4/4, schema-v4 build-stat tests 15/15, 194/194 artifact budgets, production build, 90/90 Chromium E2E in 28.0 seconds, and `git diff --check`; unrelated CVE work remains intentionally deferred.
- 2026-07-18: started the next autonomous cross-category slice: the local NanoID catalog feature plus strict lossless, bounded worker-backed JSON Prettify. Added the already lock-resolved `jsonc-parser@3.2.0` as a direct route dependency; external CVE work remains deferred.
- 2026-07-18: independent review closed NanoID pigeonhole/exact-small-namespace guidance, huge alphabet/count preflights, native input bounds, URL-safe/default copy, and JSON5 pre-allocation amplification. The new strict Long Task gate then exposed a remaining full-string scan on the main-thread worker transfer boundary; JSON/YAML transfer metadata refactoring is active rather than weakening the `<50 ms` assertion.
- 2026-07-18: completed the NanoID + strict JSON numeric-lexeme/YAML integer-preservation slice. Full-payload UTF-8 scans moved into pure-tested worker handlers while the main thread retains only bounded preview truncation; final production fixtures measure JSON 404 ms cold / 197 ms ready / 0.0 ms longest task, YAML 203 ms / 0.0 ms, and maximum NanoID generation 144 ms / 0.0 ms. Independent audit then closed YAML scalar-alias/deep-sequence/escaped-line/boundary-space allocation before serialization and stabilized the Chromium measurement window against garbage from prior discarded contexts without weakening the `<50 ms` current-interaction gate. Final gates pass: lint, dual typecheck, 546/546 unit, 15/15 build-stat, 4/4 OUI, 196/196 artifact budgets, 24,177-module production build in 19.44 s, 94/94 sequential Chromium E2E including all 87 routes, YAML repeat-each 10/10, and refreshed schema-v4 stats. External CVE work remains intentionally deferred.
- 2026-07-18: synchronized `TODO`, `FEATURES`, `FIXES`, `PERFORMANCE`, `PERSISTENCE`, experiment notes, and this journal to the final schema-v4 metrics. A separate read-only consistency audit returned clean after narrowing the claims to strict JSON numeric lexemes, YAML integer preservation, worker-side full-payload accounting, and bounded main-thread preview scanning. Repeat lint, typecheck, 546/546 unit, 15/15 build-stat, 4/4 OUI, 196/196 artifact-budget, and diff checks pass.
- 2026-07-18: started the next autonomous cross-category slice: a local three-draft JSON Schema Validator, extraction of the proven JSON/YAML one-shot worker transport, and project-owned Bcrypt/OUI protocol hardening. Ajv 8.17.1 was promoted to an exact direct route dependency; issues #368/#801 remained requirements-only and no upstream code was transferred.
- 2026-07-18: delivered the JSON Schema Validator with strict local-reference/resource/privacy contracts, prototype-safe validation semantics, and a schema/instance node-pair `allErrors` guard; delivered the shared JSON/YAML task transport; bounded and sanitized Bcrypt errors; and corrected malformed OUI response classification. Final integrated gates pass: lint, dual typecheck, 729/729 unit across 97 files, 15/15 build-stat, 4/4 OUI, 198/198 artifact budgets, 24,185-module production build in 19.86 s, 97/97 sequential Chromium E2E including all 88 routes, and `git diff --check`. The validator closure is 176,585 B raw / 53,355 B gzip, its worker is 161,471 B / 47,125 B, the nine-entry PWA is 954,481 B / 327,007 B, and the final 1 MiB flow is 246 ms cold / 205 ms ready / 0.0 ms longest task. The accepted initial-shell cost is +350 B gzip for the catalog entry while Ajv remains lazy and no new chunk exceeds 500 kB. External CVE/base-image work remains intentionally deferred.
- 2026-07-18: final independent adversarial review found and closed one medium Ajv error-amplification gap with a real branch-heavy regression. No high-severity findings remain; lower-risk `messageerror`, envelope-first decode, legacy array-record, OUI text-sanitization, and byte-metadata trust refinements are explicitly queued in `TODO` rather than hidden or overstated.
- 2026-07-18: started the next autonomous cross-category slice: third catalog feature local file hashing, same-file upload-control correctness, and compatible worker-transport hardening. The accepted implementation direction is local/session-only, explicit, fixed-chunk, worker-owned, cancellable, and measurable; no upstream code is transferred and the external CVE/base-image track remains deferred.
- 2026-07-18: independent File Hash review found that post-GC page heap alone could not prove bounded peak memory and identified exact-key, cancellation/replacement, filename-spoofing, production-window, offline-worker, clipboard, and independent-worker-budget gaps. The accepted tree closes all of them: peak page/process memory is sampled during a 256 MiB run, replacement/cancel wait for nonzero progress, wire fields are exact, 4 MiB boundaries are differential-tested, format/invisible filename characters are visible or replaced, and Workbox re-hashes offline. Browser-engine physical zero-copy is deliberately not claimed.
- 2026-07-18: completed the File Hash + compatible transport/upload slice. Final gates pass: zero-warning lint, dual typecheck, 816/816 unit across 104 files, 16/16 build-stat tests, 4/4 OUI checks, 202/202 artifact budgets, 24,192-module production build in 21.17 s, 102/102 sequential Chromium E2E including all 89 routes, and `git diff --check`. File Hash closure is 56,994 B raw / 22,189 B gzip, worker 13,583 B / 6,298 B, PWA 9 files / 956,157 B / 327,325 B, and the 256 MiB/all-three gate is 9,999 ms with 0.0 ms longest Long Task and +91,242,496 B peak sampled Chromium-process RSS. External CVE/base-image work remains intentionally deferred.
- 2026-08-07: prepared the machine-independent recovery contract in `.ai/HANDOFF.md`, corrected stale current-state architecture/toolchain guidance, and verified the live GitHub ref for `origin/feat-ai-research` at implementation checkpoint `5f7e97a`. Before the documentation-only handoff edit, the worktree was clean with no staged/untracked files, submodules, LFS objects, or machine-local project inputs.
